//dung for in cac so nguyen
#include <stdio.h>
#include <conio.h>

int main()
{
    int count;

    for(count = 1; count <= 10; count++)
        printf("%d ", count);

    printf("\n");

    getch();
    return 0;
}
